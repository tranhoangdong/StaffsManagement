@model Staff

<h1>Delete Staff</h1>

<p>are you want delete?</p>

<div>
    <h4>Staff Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Staff ID:</dt>
        <dd class="col-sm-10">@Model.StaffID</dd>
        <dt class="col-sm-2">Name:</dt>
        <dd class="col-sm-10">@Model.StaffName</dd>
        <dt class="col-sm-2">Age:</dt>
        <dd class="col-sm-10">@Model.StaffAge</dd>
        <dt class="col-sm-2">Address:</dt>
        <dd class="col-sm-10">@Model.StaffAddress</dd>
    </dl>
</div>

<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="StaffID" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-action="Index">Back to List</a>
</form>
